#!/bin/bash
set -euo pipefail
source $(dirname $0)/env.sh

echo "> Removing project tools from $CARGO_INSTALL_ROOT..."
rm -rf "$CARGO_INSTALL_ROOT"

echo "> Cleaning build artifacts..."
cargo clean || true

echo "> Teardown complete!"
echo "> Run setup-env to reinstall tools"